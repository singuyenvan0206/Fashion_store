<Window x:Class="WpfApp1.ProductManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Quản Lý Sản Phẩm - Thời Trang" Height="720" Width="1100" WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <Style TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </Window.Resources>
    
    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#D6336C" Padding="18">
            <TextBlock Text="👗 Quản Lý Sản Phẩm - Cửa Hàng Thời Trang"
                       FontSize="22"
                       FontWeight="Bold"
                       Foreground="White"
                       HorizontalAlignment="Center"/>
        </Border>

        <!-- Main -->
        <Grid Grid.Row="1" Margin="18">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="420"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Left: Form -->
            <Border Grid.Column="0" Background="White" CornerRadius="10" Padding="16" Margin="0,0,12,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
<<<<<<< HEAD
                        <TextBlock Text="📝 Thông tin sản phẩm" FontSize="18" FontWeight="Bold" Foreground="#D6336C" Margin="0,0,0,12"/>

                        <TextBlock Text="Tên sản phẩm *" FontWeight="SemiBold" Margin="0,0,0,4"/>
                        <TextBox x:Name="ProductNameTextBox" Margin="0,0,0,10"/>

                        <TextBlock Text="SKU / Mã" FontWeight="SemiBold" Margin="0,0,0,4"/>
                        <TextBox x:Name="ProductCodeTextBox" Margin="0,0,0,10"/>

                        <TextBlock Text="Danh mục" FontWeight="SemiBold" Margin="0,0,0,4"/>
                        <ComboBox x:Name="CategoryComboBox" DisplayMemberPath="Name" SelectedValuePath="Id" Margin="0,0,0,10"/>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <StackPanel Width="200">
                                <TextBlock Text="Giá (VND) *" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                <TextBox x:Name="PriceTextBox"/>
                            </StackPanel>
                            <StackPanel Margin="12,0,0,0" Width="200">
                                <TextBlock Text="Số lượng tồn *" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                <TextBox x:Name="StockQuantityTextBox"/>
                            </StackPanel>
=======
                        <TextBlock Text="Thông Tin Sản Phẩm" 
                                   FontSize="18" 
                                   FontWeight="Bold" 
                                   Foreground="#1CB5E0" 
                                   Margin="0,0,0,20"/>
                        
                        <!-- Product Name -->
                        <TextBlock Text="Tên Sản Phẩm *" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <TextBox x:Name="ProductNameTextBox" Margin="0,0,0,15"/>
                        
                        <!-- Product Code -->
                        <TextBlock Text="Mã Sản Phẩm" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <TextBox x:Name="ProductCodeTextBox" Margin="0,0,0,15"/>
                        
                        <!-- Category -->
                        <TextBlock Text="Danh Mục" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <ComboBox x:Name="CategoryComboBox" 
                                  DisplayMemberPath="Name"
                                  SelectedValuePath="Id"
                                  Margin="0,0,0,15"/>
                        
                        <!-- Price -->
                        <TextBlock Text="Giá *" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <TextBox x:Name="PriceTextBox" Margin="0,0,0,15"/>
                        
                        <!-- Stock Quantity -->
                        <TextBlock Text="Số Lượng Tồn *" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <TextBox x:Name="StockQuantityTextBox" Margin="0,0,0,15"/>
                        
                        <!-- Description -->
                        <TextBlock Text="Mô Tả" FontWeight="SemiBold" Margin="0,0,0,5"/>
                        <TextBox x:Name="DescriptionTextBox" 
                                 Height="80" 
                                 TextWrapping="Wrap" 
                                 AcceptsReturn="True"
                                 VerticalScrollBarVisibility="Auto"
                                 Margin="0,0,0,20"/>
                        
                        <!-- Buttons -->
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <Button Content="Thêm" 
                                    Width="80" 
                                    Background="#4CAF50" 
                                    Foreground="White" 
                                    Click="AddButton_Click"
                                    Margin="0,0,10,0"/>
                            <Button Content="Cập Nhật" 
                                    Width="80" 
                                    Background="#2196F3" 
                                    Foreground="White" 
                                    Click="UpdateButton_Click"
                                    Margin="0,0,10,0"/>
                            <Button Content="Xóa" 
                                    Width="80" 
                                    Background="#F44336" 
                                    Foreground="White" 
                                    Click="DeleteButton_Click"
                                    Margin="0,0,10,0"/>
                            <Button Content="Xóa Tất Cả" 
                                    Width="80" 
                                    Background="#9E9E9E" 
                                    Foreground="White" 
                                    Click="ClearButton_Click"/>
>>>>>>> parent of 97921a3 (Change some layouts)
                        </StackPanel>

                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <StackPanel Width="120">
                                <TextBlock Text="Size" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                <ComboBox x:Name="SizeComboBox" Margin="0">
                                    <ComboBoxItem>XS</ComboBoxItem><ComboBoxItem>S</ComboBoxItem><ComboBoxItem>M</ComboBoxItem><ComboBoxItem>L</ComboBoxItem><ComboBoxItem>XL</ComboBoxItem>
                                </ComboBox>
                            </StackPanel>
                            <StackPanel Margin="12,0,0,0" Width="120">
                                <TextBlock Text="Màu" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                <TextBox x:Name="ColorTextBox"/>
                            </StackPanel>
                            <StackPanel Margin="12,0,0,0" Width="150">
                                <TextBlock Text="Giới tính" FontWeight="SemiBold" Margin="0,0,0,4"/>
                                <ComboBox x:Name="GenderComboBox">
                                    <ComboBoxItem>Unisex</ComboBoxItem><ComboBoxItem>Nam</ComboBoxItem><ComboBoxItem>Nữ</ComboBoxItem>
                                </ComboBox>
                            </StackPanel>
                        </StackPanel>

                        <TextBlock Text="Thương hiệu" FontWeight="SemiBold" Margin="0,0,0,4"/>
                        <TextBox x:Name="BrandTextBox" Margin="0,0,0,10"/>

                        <TextBlock Text="Chất liệu" FontWeight="SemiBold" Margin="0,0,0,4"/>
                        <TextBox x:Name="MaterialTextBox" Margin="0,0,0,10"/>

                        <TextBlock Text="Mô tả" FontWeight="SemiBold" Margin="0,0,0,4"/>
                        <TextBox x:Name="DescriptionTextBox" Height="100" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,12"/>

                        <TextBlock Text="Ảnh sản phẩm" FontWeight="SemiBold" Margin="0,0,0,4"/>
                        <Border Height="120" Background="#FAFAFA" CornerRadius="6" BorderBrush="#E0E0E0" BorderThickness="1" Margin="0,0,0,12" VerticalAlignment="Top">
                            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock Text="Kéo thả ảnh hoặc click để chọn" Foreground="#888"/>
                            </StackPanel>
                        </Border>

                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,8,0,0">
                            <Button x:Name="AddButton" Content="➕ Thêm" Width="90" Height="36" Background="#10B981" Foreground="White" Click="AddButton_Click" Margin="0,0,8,0"/>
                            <Button x:Name="UpdateButton" Content="✏️ Cập nhật" Width="110" Height="36" Background="#3B82F6" Foreground="White" Click="UpdateButton_Click" Margin="0,0,8,0"/>
                            <Button x:Name="DeleteButton" Content="🗑️ Xóa" Width="90" Height="36" Background="#EF4444" Foreground="White" Click="DeleteButton_Click" Margin="0,0,8,0"/>
                            <Button x:Name="ClearButton" Content="🔄 Làm mới" Width="100" Height="36" Click="ClearButton_Click"/>
                        </StackPanel>

                        <Button x:Name="DeleteAllButton" Content="⚠️ Xóa tất cả sản phẩm" Background="#B91C1C" Foreground="White" Height="36" Margin="0,12,0,0" Click="DeleteAllButton_Click"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <!-- Right: List / Search -->
            <Border Grid.Column="1" Background="White" CornerRadius="10" Padding="12">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,12">
                        <TextBox x:Name="SearchTextBox" Width="360" Height="34" TextChanged="SearchTextBox_TextChanged" />
                        <Button Content="🔎" Width="44" Height="34" Margin="8,0,0,0" Click="SearchButton_Click"/>
                        <TextBlock x:Name="StatusTextBlock" VerticalAlignment="Center" Margin="12,0,0,0" Foreground="#666"/>
                    </StackPanel>

                    <DataGrid x:Name="ProductDataGrid" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False" SelectionMode="Single" HeadersVisibility="Column"
                              SelectionChanged="ProductDataGrid_SelectionChanged">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="#" Binding="{Binding Id}" Width="60" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Tên" Binding="{Binding Name}" Width="*" IsReadOnly="True"/>
                            <DataGridTextColumn Header="SKU" Binding="{Binding Code}" Width="140" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Danh mục" Binding="{Binding CategoryName}" Width="140" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Giá" Binding="{Binding Price, StringFormat=N0}" Width="120" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Tồn" Binding="{Binding StockQuantity}" Width="80" IsReadOnly="True"/>
                            <DataGridTextColumn Header="Mô tả" Binding="{Binding Description}" Width="200" IsReadOnly="True"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
                    </StackPanel>
                </ScrollViewer>
            </Border>
            
            <!-- Product List -->
            <Border Grid.Column="1" 
                    Background="White" 
                    CornerRadius="10" 
                    Padding="20">
                <StackPanel>
                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <TextBlock Grid.Column="0" 
                                   Text="Danh Sách Sản Phẩm" 
                                   FontSize="18" 
                                   FontWeight="Bold" 
                                   Foreground="#1CB5E0"/>
                        
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <TextBox x:Name="SearchTextBox" 
                                     Width="200" 
                                     Margin="0,0,10,0"
                                     TextChanged="SearchTextBox_TextChanged"
                                     VerticalContentAlignment="Center"/>
                            <Button Content="Tìm Kiếm" 
                                    Width="80" 
                                    Background="#FF9800" 
                                    Foreground="White"
                                    Click="SearchButton_Click"/>
                        </StackPanel>
                    </Grid>
                    
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                        <DataGrid x:Name="ProductDataGrid" 
                                  AutoGenerateColumns="False" 
                                  IsReadOnly="True"
                                  SelectionMode="Single"
                                  SelectionChanged="ProductDataGrid_SelectionChanged"
                                  Height="400">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                            <DataGridTextColumn Header="Tên" Binding="{Binding Name}" Width="150"/>
                            <DataGridTextColumn Header="Mã" Binding="{Binding Code}" Width="100"/>
                            <DataGridTextColumn Header="Danh Mục" Binding="{Binding CategoryName}" Width="100"/>
                            <DataGridTextColumn Header="Giá" Binding="{Binding Price, StringFormat=C}" Width="80"/>
                            <DataGridTextColumn Header="Tồn" Binding="{Binding StockQuantity}" Width="60"/>
                            <DataGridTextColumn Header="Mô Tả" Binding="{Binding Description}" Width="200"/>
                        </DataGrid.Columns>
                        </DataGrid>
                    </ScrollViewer>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
